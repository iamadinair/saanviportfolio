diff --git a/src/components/PhotoCarousel.astro b/src/components/PhotoCarousel.astro
new file mode 100644
index 0000000..279bc92
--- /dev/null
+++ b/src/components/PhotoCarousel.astro
@@ -0,0 +1,74 @@
+---
+import { siteConfig } from '../data/site.config.js';
+const carouselItems = siteConfig.gallery.filter((item) => item.orientation !== 'landscape');
+---
+<section id="carousel" class="carousel-section reveal">
+  <div class="carousel-wrap">
+    <button class="carousel-arrow carousel-arrow--prev" type="button" aria-label="Scroll to previous photo">
+      <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6"/></svg>
+    </button>
+
+    <div class="carousel-track" id="carousel-track">
+      {carouselItems.map((item) => (
+        <figure class:list={['carousel-card', 'reveal', { 'carousel-card--placeholder': item.placeholder }]}>
+          {item.placeholder ? (
+            <div class="carousel-frame placeholder">
+              <span>More photos<br />coming soon</span>
+            </div>
+          ) : (
+            <div class="carousel-frame">
+              <img src={item.src} alt={item.alt} loading="lazy" />
+            </div>
+          )}
+          <figcaption>
+            <span class="cap-index">{item.index}</span>
+            <span class="cap-title">{item.caption}</span>
+          </figcaption>
+        </figure>
+      ))}
+    </div>
+
+    <button class="carousel-arrow carousel-arrow--next" type="button" aria-label="Scroll to next photo">
+      <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
+    </button>
+  </div>
+</section>
+
+<script>
+  const track = document.getElementById('carousel-track');
+  const prevBtn = document.querySelector('.carousel-arrow--prev');
+  const nextBtn = document.querySelector('.carousel-arrow--next');
+
+  if (track) {
+    // Turn vertical mouse-wheel motion into horizontal carousel scrolling
+    // while the carousel still has room to move; once it's fully scrolled
+    // in that direction, let the wheel resume scrolling the page normally.
+    track.addEventListener(
+      'wheel',
+      (e) => {
+        const atStart = track.scrollLeft <= 0;
+        const atEnd = track.scrollLeft + track.clientWidth >= track.scrollWidth - 1;
+        const scrollingDown = e.deltaY > 0;
+        const scrollingUp = e.deltaY < 0;
+
+        if ((scrollingDown && !atEnd) || (scrollingUp && !atStart)) {
+          e.preventDefault();
+          track.scrollLeft += e.deltaY * 1.4;
+        }
+      },
+      { passive: false }
+    );
+
+    const cardWidth = () => {
+      const card = track.querySelector('.carousel-card');
+      return card ? card.getBoundingClientRect().width + 20 : 320;
+    };
+
+    prevBtn?.addEventListener('click', () => {
+      track.scrollBy({ left: -cardWidth(), behavior: 'smooth' });
+    });
+    nextBtn?.addEventListener('click', () => {
+      track.scrollBy({ left: cardWidth(), behavior: 'smooth' });
+    });
+  }
+</script>
diff --git a/src/components/PhotoSequence.astro b/src/components/PhotoSequence.astro
index e49fe0c..4259280 100644
--- a/src/components/PhotoSequence.astro
+++ b/src/components/PhotoSequence.astro
@@ -1,24 +1,13 @@
 ---
 import { siteConfig } from '../data/site.config.js';
-const { gallery } = siteConfig;
+const landscapeItems = siteConfig.gallery.filter((item) => item.orientation === 'landscape');
 ---
 <section id="gallery" class="photo-sequence">
-  {gallery.map((item) => (
-    <figure class:list={[
-      'photo-block',
-      'reveal',
-      item.orientation === 'landscape' ? 'photo-block--landscape' : 'photo-block--portrait',
-      { 'photo-block-placeholder': item.placeholder },
-    ]}>
-      {item.placeholder ? (
-        <div class="photo-block-frame placeholder">
-          <span>More photos<br />coming soon</span>
-        </div>
-      ) : (
-        <div class="photo-block-frame">
-          <img src={item.src} alt={item.alt} />
-        </div>
-      )}
+  {landscapeItems.map((item) => (
+    <figure class="photo-block reveal photo-block--landscape">
+      <div class="photo-block-frame">
+        <img src={item.src} alt={item.alt} />
+      </div>
       <figcaption>
         <span class="cap-index">{item.index}</span>
         <span class="cap-title">{item.caption}</span>
diff --git a/src/pages/index.astro b/src/pages/index.astro
index 5c01383..dba33ca 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -3,6 +3,7 @@ import PortfolioLayout from '../layouts/PortfolioLayout.astro';
 import Nav from '../components/Nav.astro';
 import Hero from '../components/Hero.astro';
 import PhotoSequence from '../components/PhotoSequence.astro';
+import PhotoCarousel from '../components/PhotoCarousel.astro';
 import Bio from '../components/Bio.astro';
 import CompCard from '../components/CompCard.astro';
 import ContactForm from '../components/ContactForm.astro';
@@ -13,6 +14,7 @@ import { siteConfig } from '../data/site.config.js';
   <Nav />
   <Hero />
   <PhotoSequence />
+  <PhotoCarousel />
   <Bio />
   {siteConfig.showCompCard && <CompCard />}
   <ContactForm />
diff --git a/src/styles/global.css b/src/styles/global.css
index 72f5c93..801472e 100644
--- a/src/styles/global.css
+++ b/src/styles/global.css
@@ -206,32 +206,89 @@ header.nav {
 .photo-block--landscape .cap-index,
 .photo-block--landscape .cap-title { color: #fff; }
 
-/* ---- Portrait: framed card treatment ---- */
-.photo-block--portrait {
-  padding: 56px 6vw 8px;
+.cap-index {
+  font-family: var(--font-display);
+  font-size: 14px;
+  opacity: 0.8;
 }
 
-.photo-block--portrait .photo-block-frame {
-  max-width: 460px;
+.cap-title {
+  font-family: var(--font-display);
+  font-size: 22px;
+  font-weight: 500;
+}
+
+/* ===================================================================
+   CAROUSEL (images 2–5: horizontal, wheel-scrollable)
+=================================================================== */
+.carousel-section {
+  padding: 72px 0;
+}
+
+.carousel-wrap {
+  position: relative;
+}
+
+.carousel-wrap::before,
+.carousel-wrap::after {
+  content: '';
+  position: absolute;
+  top: 0;
+  bottom: 0;
+  width: 8vw;
+  z-index: 2;
+  pointer-events: none;
+}
+
+.carousel-wrap::before {
+  left: 0;
+  background: linear-gradient(to right, var(--bg), transparent);
+}
+
+.carousel-wrap::after {
+  right: 0;
+  background: linear-gradient(to left, var(--bg), transparent);
+}
+
+.carousel-track {
+  display: flex;
+  gap: 24px;
+  overflow-x: auto;
+  scroll-snap-type: x proximity;
+  padding: 8px 8vw 28px;
+  scrollbar-width: thin;
+}
+
+@media (min-width: 900px) {
+  .carousel-track { padding-left: 4rem; padding-right: 4rem; }
+}
+
+.carousel-card {
+  flex: 0 0 auto;
+  width: min(72vw, 360px);
+  scroll-snap-align: center;
+  margin: 0;
+}
+
+.carousel-frame {
   aspect-ratio: 4 / 5;
-  margin: 0 auto;
   border-radius: 20px;
   background: var(--surface);
   overflow: hidden;
   box-shadow: var(--shadow);
 }
 
-.photo-block--portrait .photo-block-frame img {
+.carousel-frame img {
   width: 100%;
   height: 100%;
   object-fit: cover;
   object-position: center top;
-  transition: transform 1.2s cubic-bezier(.2,.8,.2,1);
+  transition: transform 0.6s cubic-bezier(.2,.8,.2,1);
 }
 
-.photo-block--portrait:hover .photo-block-frame img { transform: scale(1.03); }
+.carousel-card:hover .carousel-frame img { transform: scale(1.05); }
 
-.photo-block--portrait.photo-block-placeholder .photo-block-frame {
+.carousel-card--placeholder .carousel-frame {
   display: flex;
   align-items: center;
   justify-content: center;
@@ -242,34 +299,44 @@ header.nav {
   border: 1.5px dashed var(--line);
 }
 
-.photo-block--portrait figcaption {
-  max-width: 460px;
-  margin: 16px auto 0;
+.carousel-card figcaption {
+  margin: 14px 4px 0;
   display: flex;
   align-items: baseline;
-  justify-content: center;
   gap: 10px;
   color: var(--ink-soft);
 }
 
-.photo-block--portrait .cap-index {
-  color: var(--accent-deep);
-}
+.carousel-card .cap-index { color: var(--accent-deep); }
+.carousel-card .cap-title { color: var(--ink); font-size: 19px; }
 
-.photo-block--portrait .cap-title {
+.carousel-arrow {
+  position: absolute;
+  top: 42%;
+  z-index: 3;
+  width: 44px;
+  height: 44px;
+  border-radius: 50%;
+  border: 1.5px solid var(--line);
+  background: var(--bg);
   color: var(--ink);
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  cursor: pointer;
+  transition: transform 0.2s ease, background 0.2s ease;
 }
+.carousel-arrow:hover { background: var(--surface); transform: translateY(-50%) scale(1.06); }
+.carousel-arrow--prev { left: 1vw; transform: translateY(-50%); }
+.carousel-arrow--next { right: 1vw; transform: translateY(-50%); }
 
-.cap-index {
-  font-family: var(--font-display);
-  font-size: 14px;
-  opacity: 0.8;
+@media (min-width: 900px) {
+  .carousel-arrow--prev { left: 1.25rem; }
+  .carousel-arrow--next { right: 1.25rem; }
 }
 
-.cap-title {
-  font-family: var(--font-display);
-  font-size: 22px;
-  font-weight: 500;
+@media (max-width: 700px) {
+  .carousel-arrow { display: none; }
 }
 
 /* ===================================================================
